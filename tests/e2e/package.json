{
  "name": "invctl-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "Browser E2E suite for invctl. Test-time dependency only -- never part of the Go build. See docs/E2E.md before running.",
  "type": "module",
  "license": "AGPL-3.0-only",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
